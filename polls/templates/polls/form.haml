%h2 {{poll.title}}
%p {{poll.intro}}

%form{'action':'/polls/vote/{{poll.id}}/','method':'post'}
  %fieldset
    - csrf_token
    - for option in poll.choice_set.all
      .control-group
        .controls
          %label.radio
            %input.control-label{'type':'radio', 'name':'choice', 'value':'{{option.id}}'}
            {{option}}

    %button.btn
      Stem!